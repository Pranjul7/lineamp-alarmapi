
# import ddlfs.views
# import ddlfs.api_views
from django.urls import path, include
from django.contrib import admin
from rest_framework.routers import DefaultRouter
#
# app_name = 'ddlfs'
#
# router = DefaultRouter()
# router.register('', AlarmViewSet, basename='alarm')


# urlpatterns = router.urls
#
# urlpatterns = [
    # path('api/v1/devices/', .api_views.DeviceList.as_view()),
    # path('api/v1/alarms/', .api_views.AlarmList.as_view()),
    # path('alarm/', include('ddlfs.url', namespace='alarm')),
    # path('api/', include(router.urls))

# ]
